[pytest]
testpaths = tests
# The shared client lives one level up and is not installed: the plugins vendor it
# rather than depend on it, so the tests have to find it the same way.
pythonpath = ../python
